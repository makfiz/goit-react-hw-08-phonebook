export const contactsInitialState = {
    items: [],
    isLoading: false,
    error: null,
    selectedContact:null,
    selectedContactIsEdited:false,
}

export const filterInitialState = {
  value:"" 
};